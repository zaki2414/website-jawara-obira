TODO — ITEM YANG DIKETAHUI BELUM SELESAI
================================================================================
Disusun: 2026-09-03, berdasarkan investigasi langsung ke kode (git status, grep,
ls node_modules, dsb) — bukan asumsi. Update ulang tanggal di atas kalau item
di bawah sudah dikerjakan/diverifikasi lagi.

1) PLAYWRIGHT BELUM TERPASANG
   - `package.json` sudah mencantumkan `"playwright": "^1.62.1"`.
   - TAPI `node_modules/@playwright` tidak ada — berarti belum pernah `npm install`
     untuk dependency ini, atau node_modules sempat di-reset tanpa reinstall.
   - Dampak: automated browser testing (buka situs sendiri, screenshot, cek tiap
     halaman) belum bisa jalan sama sekali sampai ini dibereskan.
   - Perbaikan: `npm install`, lalu `npx playwright install` untuk browser binary-nya.

2) REDESIGN HOME PAGE — SUDAH JALAN TAPI BELUM TUNTAS & BELUM DI-COMMIT
   - Sistem scroll-reveal GSAP ScrollTrigger baru (`lib/scrollReveal.ts` +
     `components/shared/Reveal.tsx`) dan odometer statistik
     (`components/home/CounterNumber.tsx`) sudah dibuat.
   - `KKNSection.tsx`, `PotensiBento.tsx`, `StatsSection.tsx` sudah direfactor
     besar-besaran mengikuti sistem baru ini.
   - `HeroSection.tsx` HAMPIR TIDAK TERSENTUH (cuma +2/-1 baris) — kemungkinan
     besar ini bagian yang belum digarap ulang mengikuti sistem reveal baru,
     padahal instruksi awal minta "rombak" menyeluruh termasuk hero.
   - SEMUA perubahan di atas (16 file modified + 4 file baru + rename gambar
     hero) MASIH UNCOMMITTED per 2026-09-03. Risiko hilang kalau ada masalah
     sebelum sempat di-commit. Prioritas: selesaikan HeroSection dulu, baru
     commit sekali sebagai satu unit kerja yang utuh.

3) MIDDLEWARE BELUM MIGRASI KE `proxy.ts`
   - Next.js 16 sudah men-deprecate convention file `middleware.ts` — warning
     ini muncul setiap kali `next dev` dijalankan.
   - `middleware.ts` (guard /admin & /login) masih pakai convention lama.
   - Belum ada `proxy.ts` di root project.

4) SISA `catch (err: any)` YANG BELUM DI-NARROW
   - `app/auth/callback/route.ts:45`
   - `app/api/toga/plants/route.ts:16`
   - Ganti ke `catch (err: unknown)` + narrowing `err instanceof Error` sesuai
     aturan di CLAUDE.md §4.1. (Sudah banyak berkurang dari klaim lama 35 lokasi
     jadi cuma 2 — tinggal dua ini.)

5) SERVICE ROLE KEY SALAH PREFIX DI .env.local
   - `NEXT_PUBLIC_SUPABASE_SERVICE_ROLE_KEY` masih pakai prefix `NEXT_PUBLIC_`.
   - Saat ini TIDAK dipakai di kode manapun (aman untuk sekarang), tapi
     penamaannya menyesatkan — kalau nanti ada yang pakai tanpa sadar prefix
     itu bikin Next.js embed value-nya ke client bundle.
   - Perbaikan: rename ke `SUPABASE_SERVICE_ROLE_KEY` (tanpa prefix) di
     `.env.local` dan tempat env var itu di-set (Vercel dsb), pastikan tidak
     ada referensi yang patah.

6) GITHUB ACTIONS KEEP-ALIVE — SUDAH DI-COMMIT LOKAL, BELUM DI-PUSH
   - File `.github/workflows/supabase-keepalive.yml` sudah dibuat & di-commit
     (commit lokal `552bd28`), tapi per 2026-09-03 belum ada di `origin/master`.
   - User sudah mengisi 2 repo secret (`SUPABASE_URL`, `SUPABASE_ANON_KEY`) di
     GitHub, tapi belum berguna sampai commit ini di-push.
   - Setelah push: trigger manual sekali lewat tab Actions → "Run workflow"
     untuk pastikan sukses sebelum mengandalkan jadwal otomatisnya (Senin &
     Kamis 03:00 UTC).

7) CLAUDE.md SUDAH KETINGGALAN ZAMAN DARI KODE SEBENARNYA
   - Banyak item di bagian "⚠ Utang Teknis" CLAUDE.md ternyata SUDAH selesai
     (dan sudah ter-commit di commit f72fcd1), tapi dokumennya tidak diupdate:
     ui/button.tsx, adopsi ui/card+badge, konsolidasi Entity* shared components,
     constants/nav.ts, loading.tsx+error.tsx per-route, generateMetadata,
     useReducedMotion, dan theming admin (ternyata sudah jadi sistem sadar,
     bukan "belum diputuskan" seperti tertulis).
   - Perlu direvisi supaya sesi LLM berikutnya tidak salah kerja ulang sesuatu
     yang sudah selesai, atau salah menganggap sesuatu masih "utang" padahal
     sudah fixed.

8) BELUM ADA RENCANA PASCA-KKN (HANDOFF)
   - Tidak ada dokumen manapun (`context.txt`, `ROADMAP.md`, dst) yang membahas
     siapa yang pegang akses dashboard Supabase & repo GitHub setelah tim KKN
     selesai bertugas.
   - Relevan karena misi proyek ini eksplisit "arsip digital jangka panjang",
     tapi Supabase free-tier auto-pause project setelah 7 hari tanpa aktivitas
     (item #6 di atas cuma mitigasi teknis, bukan pengganti rencana handoff
     yang sebenarnya — siapa yang punya login, siapa yang bayar kalau upgrade
     ke Pro plan nanti, dsb).

================================================================================
Cara pakai file ini: sebelum menganggap sesuatu "belum dikerjakan", verifikasi
ulang ke kode (grep/ls/git status) — jangan percaya daftar ini mentah-mentah
kalau sudah lama sejak tanggal sinkronisasi di atas, karena progress bisa
berubah cepat.
