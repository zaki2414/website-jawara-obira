import { RotatingHiasanBackground } from "@/components/shared/RotatingHiasanBackground";

/** Identitas ornamen halaman KKN (hub, proker, tim, jurnal) — Hiasan 4.svg. */
export function KknPageBackground() {
  return <RotatingHiasanBackground hiasan={4} />;
}
