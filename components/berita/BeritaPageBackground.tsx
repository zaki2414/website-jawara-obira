import { RotatingHiasanBackground } from "@/components/shared/RotatingHiasanBackground";

/** Identitas ornamen halaman berita — Hiasan 1.svg. */
export function BeritaPageBackground() {
  return <RotatingHiasanBackground hiasan={1} />;
}
